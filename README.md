# AI-Powered-Resume-Builder
A full-stack Resume Builder web application that allows users to upload or manually create resumes and generate ATS-optimized documents with professional formatting. The system parses resume data, calculates ATS scores using a free ATS API, and generates structured resumes using pre-integrated LaTeX templates (e.g., AutoCV).
